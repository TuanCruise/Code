//using HttpClientHelper;
using Newtonsoft.Json;
using System;
using System.Threading.Tasks;

namespace WebApi.Common.ExternalService
{
    public class IdentityApiService
    {
        //private IHttpClient _httpClient;
        //private string _SSOBaseUrl;
        //private string _SSOApiBaseUrl;
        //private string _authToken;
        //public IdentityApiService(IHttpClient httpClient)
        //{
        //    _httpClient = httpClient;
        //    //_SSOApiBaseUrl = WebConfigurationManager.AppSettings["SsoService"];
        //    var authUname = "";//WebApiCore.Utilities.EncryptDecrypt.Instance.Decrypt(WebConfigurationManager.AppSettings["UsernameAuthen"]);
        //    var authPass = "";// WebApiCore.Utilities.EncryptDecrypt.Instance.Decrypt(WebConfigurationManager.AppSettings["PasswordAuthen"]);
        //    if (!string.IsNullOrEmpty(authUname))
        //    {
        //        var textBytes = System.Text.Encoding.UTF8.GetBytes(authUname + ":" + authPass);
        //        _authToken = Convert.ToBase64String(textBytes);
        //    }
        //}

    }
}